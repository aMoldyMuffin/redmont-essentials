# Cloudflare Pages — static site (no build step)
# For Git deploy: leave Build command EMPTY in the Cloudflare dashboard.
# Manual deploy only: npx wrangler pages deploy . --project-name=redmont-essentials

name = "redmont-essentials"
compatibility_date = "2024-01-01"
pages_build_output_dir = "."
